/* import { db } from "../../database/mysql/promises/mysql.utils";

export async function runQuery(query: string): Promise<any> {
  try {
    return await db(query);
  } catch (error) {
    throw new Error("Error in connection database");
  }
}
 */
